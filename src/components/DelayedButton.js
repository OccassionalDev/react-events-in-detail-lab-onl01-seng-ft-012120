import React from 'react'

class DelayedButton extends React.Component {
  onDelayedClick = event => {
    
  }
  
  render() {
    return (
      <button onClick={ this.DelayedButton}>Button 2</button>
      )
  }
}

export default DelayedButton